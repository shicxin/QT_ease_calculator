#include "newlineedit.h"

void NewlineEdit::keyPressEvent(QKeyEvent *event)
{
    // 不调用父类的键盘事件处理函数
    // QLineEdit::keyPressEvent(event);
}

void NewlineEdit::keyReleaseEvent(QKeyEvent *event)
{
    // 不调用父类的键盘事件处理函数
    // QLineEdit::keyReleaseEvent(event);
}

